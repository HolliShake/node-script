import ( Dog ) from "./test.ns";

var DATA i32 = 100, TYPE str = "bool", PascalTest i8 = 10;

struct Person {
    Name str;
    Age  i32;
    Pet  Dog;
}

struct King {
    Name str;
    Age  i32;
    Pet  Dog;
}

define (p Person) GetPet() Dog {

}

define main(args [str]) i32 {
    local x i32, y i64 = 10000;
}