import ( Dog ) from "./test.ns";

var DATA i32 = 100, TYPE str = "true", PascalTest i8 = 10;

struct Person {
    Name str;
    Age  i32;
    Pet  Dog;
}

struct King {
    Name str;
    Age  i32;
    Pet  Dog;
}

define (p Person) GetPet() Dog {
    a, b, c := 100, 200, "Hola!";
    d := a;
}

const Kill str = "adadaasdas";

define main(args [str]) i32 {
    const g i32 = 102;
}
